import * as React from 'react';
import NumbersGame from './NumbersGame';

export default (props: any) =>
  <div className="container">
    <h2>Arrastrar</h2>

    <NumbersGame title="Ordená las cajas de mayor a menor"/>
  </div>